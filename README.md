# task_scheduler
task_scheduler

just run:

    ./ss.sh {yyyymmdd} {mode}


mode: 

    0: run under the config file;
    1: run from the db status(from the last break point).
    
config file:

    ./cfg/task_info.xls
    